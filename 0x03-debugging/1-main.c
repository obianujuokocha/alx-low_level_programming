#include <stdio.h>

/**
 * main - causes an infinite loop
 * Return: 0
 */

int main(void)
{
	int 1;

	printf("infinite loop incoming :(\n");

	i = 0;
	/*
	 * while (1 < 10)
	 * {
	 *	putchar(1);
	 * }
	 */

	printf("inifinite loop avoided! \\o/\n");

	return (0);
}
